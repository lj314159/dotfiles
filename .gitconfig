[user]
    email = jones.layne555@gmail.com
[alias]
    s = status -s
    pretty = log --author='Layne Jones' --pretty=oneline --no-merges
    pom = pull origin main
    root = rev-parse --show-toplevel
    am = commit --amend --no-edit
[core]
    editor = vim
    whitespace = -trailing-space
[pull]
    ff = true
